const routes = {
    home: '/',
    profile: '/profile', // /profile/:user_id
    products: '/products',
    admin: '/admin',
    login: '/login',
    register: '/register',
    cart: '/cart',
    checkout: '/checkout',
    chat: '/chat'
};

export default routes;
